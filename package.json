{
  "name": "deeptalk-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "DeepTalk 前端 UI(卡片流 / 双态动效 / 影子回放 / 导入 / 导出面板)。零依赖,纯 ES Module,可用浏览器直接演示。",
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "serve": "node scripts/serve.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "dev:vite": "vite",
    "test": "node test/smoke.mjs"
  },
  "license": "GPL-3.0",
  "dependencies": {
    "@tauri-apps/api": "^2",
    "pdfkit": "^0.19.1",
    "pureimage": "^0.4.20"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "jsdom": "^24",
    "vite": "^6"
  }
}
